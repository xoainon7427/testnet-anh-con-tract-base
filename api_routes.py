server:
  port: 8080
  environment: production
database:
  connection: localhost
# Cấu hình thông số cho máy chủ (Server)
server:
  port: 8080                # Cổng (Port) mà ứng dụng sẽ chạy và lắng nghe các yêu cầu
  environment: production   # Môi trường thực thi (production nghĩa là môi trường vận hành thực tế)

# Cấu hình kết nối tới cơ sở dữ liệu (Database)
database:
  connection: localhost     # Địa chỉ máy chủ database (localhost nghĩa là database nằm ngay trên cùng máy này)
