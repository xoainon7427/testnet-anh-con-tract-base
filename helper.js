#include <iostream>
int main() {
    std::cout << "Hello from C++!" << std::endl;
    return 0;
}
#include <iostream> // Khai báo thư viện iostream để sử dụng các tính năng nhập/xuất (như cout)

// Hàm main là hàm khởi đầu, nơi mọi chương trình C++ bắt đầu thực thi
int main() {
    // std::cout: Lệnh dùng để xuất (in) dữ liệu ra màn hình console
    // <<: Toán tử chèn, dùng để gửi dữ liệu vào luồng xuất
    // std::endl: Ký tự xuống dòng và làm sạch bộ nhớ đệm (buffer)
    std::cout << "Hello from C++!" << std::endl;

}
